{% if cookiecutter.profile == "full-stack" -%}
# Browser-safe values only. Never add a secret or service-role key with a VITE_ prefix.
VITE_SUPABASE_URL=http://127.0.0.1:55321
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
{% elif cookiecutter.profile == "python-api" -%}
{{ cookiecutter.package_name | upper }}_ENVIRONMENT=local
{{ cookiecutter.package_name | upper }}_LOG_LEVEL=INFO
{% else -%}
# Add provider-specific keys only in .env, never here or in source control.
MODEL_PROVIDER=
{% endif %}
# Keep real credentials in ignored environment files.
